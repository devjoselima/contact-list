class Contact {
  name: string;
  email: string;
  number: number;
  id: number;

  constructor(name: string, email: string, number: number, id: number) {
    this.name = name;
    this.email = email;
    this.number = number;
    this.id = id;
  }
}

export default Contact;
